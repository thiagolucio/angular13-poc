import { ColorDeleteDirective } from './color-delete.directive';

describe('ColorDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorDeleteDirective();
    expect(directive).toBeTruthy();
  });
});
